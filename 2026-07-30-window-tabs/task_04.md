---
status: completed
title: Docs: window-management skill + config reference
type: docs
complexity: low
---

# Task 4: Docs: window-management skill + config reference

## Overview

Documents the tab model for agents and operators: the official bundled skill reference (`skills/compozy/`) learns the tab operating model, commands, verbs, tools, hooks, and error contracts; the site's configuration reference gains the two new keys with their write-time/non-retroactive semantics. Generated CLI pages are owned by task_02's codegen — this task covers the hand-authored surfaces only.

<critical>
- ALWAYS READ the PRD, the TechSpec, and their catalogs (`_user_stories.md`, `_tests.md`) before starting
- REFERENCE TECHSPEC for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — implement every test case assigned in ## Tests
</critical>

<requirements>
1. MUST update `skills/compozy/references/window-management.md` with: the tab model (tab = grouped window; deck ≥2; per-client active vs durable last-active), the five new commands + three extended payloads with the **activate** public noun and `set_active` internal-ID note, CLI examples for group/activate/pin/reopen and the extended open/navigate/close flags, the five hook events, the two new error codes, the two config keys, and the v3 discard policy (operator-facing: what happens to pre-tabs arrangement and v2 layout profiles).
2. MUST add `nav_stack_limit` and `closed_entry_limit` to `packages/site/content/docs/configuration/config-toml.mdx` (sample + reference rows, documenting write-time caps and non-retroactive lowering) and to `lifecycle-matrix.mdx` (`live`/`live`/`none` rows).
3. MUST keep runtime truth authoritative: every documented command/flag/error string verified against the shipped task_02 surface (no aspirational wording), per `COPY.md` claim standards and the glossary (`capability` discipline; window-manager vocabulary).
4. SHOULD keep the skill reference lean (context-budget rule) — extend existing sections instead of duplicating structure.
</requirements>

## Subtasks

- [x] 4.1 Update the skill reference's Operating Model, CLI, Native Tools, HTTP/UDS/Stream, and Configuration/Hooks sections with the tab surface
- [x] 4.2 Document the v3 discard + v2 profile rejection as operator guidance (Raw Layout Recovery section)
- [x] 4.3 Add the config keys to `config-toml.mdx` (sample + reference) and `lifecycle-matrix.mdx`
- [x] 4.4 Verify every documented string against the live CLI/HTTP output from task_02 (spot-run the verbs)

## Implementation Details

Section map of the skill reference and the exact site paths are in the transcript exploration report (§17): `skills/compozy/references/window-management.md` (sections: Operating Model :17, CLI :42, Native Tools :127, HTTP/UDS/Stream :134, Raw Layout Recovery :173, Configuration And Hooks :195); `packages/site/content/docs/configuration/config-toml.mdx` (table row :34, sample :140-168, reference §`[window_manager]` :804-848); `lifecycle-matrix.mdx:60`.

### Relevant Files

- `skills/compozy/references/window-management.md` — the bundled skill reference agents read
- `packages/site/content/docs/configuration/config-toml.mdx` — config sample + reference rows
- `packages/site/content/docs/configuration/lifecycle-matrix.mdx` — key lifecycle rows

### Dependent Files

- `packages/site/content/docs/cli/window/*.mdx` — generated by task_02; cross-links only
- `docs/qa/scenarios/MS-configure-window-manager.md` — reset owned by task_05 (config docs are its evidence source)

### Related ADRs

- [ADR-009](adrs/adr-009.md) — activate naming; [ADR-012](adrs/adr-012.md) — discard policy wording; [ADR-013](adrs/adr-013.md) — reopen semantics

### Web/Docs Impact

- `web/`: none — checked: no web code consumes these docs.
- `packages/site`: the two configuration MDX pages above; site build + link-check gate them.
- QA impact: none directly (docs describe task_01/02 behavior; their scenarios are reset by those tasks' entries via task_05). `MS-configure-window-manager` walk (task_06) uses these pages as its reference.

### Extensibility / Agent Manageability / Config Lifecycle

- Extensibility: official Compozy skill updated (required by the repo rule when tool IDs/CLI paths/hook events change). Checked/no impact: extension manifests, bundles, registries, bridge SDKs, MCP sidecars.
- Agent manageability: documentation of the agent path is part of the manageability contract (internal/CLAUDE.md "docs that describe the agent path") — this task closes it.
- Config lifecycle: docs + examples halves of the two keys' lifecycle (structs/validation/tests landed in task_01).

## Deliverables

- Updated skill reference covering the full tab surface with runtime-verified strings
- Config reference + lifecycle matrix rows for both keys
- No new automated tests — rationale: `make codegen-check` owns generated-doc drift, the site build + link gates own MDX integrity, and prose is not a product contract (test-placement rule); documented manual verification of CLI strings in Completion Notes
- Every test case assigned in `## Tests` implemented and passing **(REQUIRED — none assigned; see rationale above)**

## Tests

No `_tests.md` IDs assigned. Owning gates: `make codegen-check` (generated CLI docs drift), site build + link-check (MDX), `bun-lint` (formatting). Manual evidence required in Completion Notes: verbatim CLI output for `compozy window group|activate|pin|reopen --help` matching the documented flags.

## Success Criteria

- Skill reference and config pages match shipped runtime behavior exactly (spot-run evidence recorded)
- Site build green; no broken links; `make gate` no-op or green for the docs lane
- Glossary/COPY.md conformance: no forbidden synonyms, no aspirational claims

## Completion Notes

- The bundled skill now covers the tab model, five new commands, extended payloads, public
  `activate` naming, native tools, HTTP/UDS/stream contracts, hook events, error codes, v3 raw-layout
  recovery, and v2 rejection.
- `config-toml.mdx` and `lifecycle-matrix.mdx` document both write-time limits and their
  non-retroactive lowering behavior.
- Live help for `compozy window group|activate|pin|reopen --help` was captured verbatim at
  `evidence/task_04/cli-help.txt`; codegen drift, site build, lint, typecheck, tests, and the
  automatically escalated full `make gate` passed.

Compozy Impact Audit:

- Native tools: documented `compozy__window_group`, `window_reorder`, `window_activate`,
  `window_pin`, and `window_reopen` against the shipped descriptors; no runtime mutation here.
- Extensibility and hooks: documented the five shipped tab hook events; extension manifests,
  bundles, registries, bridge SDKs, and MCP sidecars were checked and are unchanged.
- Workspace data isolation: documentation preserves workspace-scoped routes and per-client
  presentation semantics; no data path changed.
- Official Compozy skill: updated the canonical `skills/compozy/` references for every public tab
  contract introduced by Tasks 01–02.
