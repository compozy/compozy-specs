---
status: completed
title: Web + catalog: format badges, skipped components, feed hard cut
type: frontend
complexity: medium
---

# Task 5: Web + catalog: format badges, skipped components, feed hard cut

## Overview

The web half of the frozen surface: the `agent plugin` format badge lands as the sixth pill variant across marketplace cards, detail views, and settings; the kit inventory panel gains the Skipped section rendering recorded and live diagnostics; and the curated catalog feed gains the optional `format` field as a hard cut (reader + feeds + fixtures + projection in one change). One vertical slice from the Go feed reader through the daemon projection to the React components and Playwright journeys.

<critical>
- ALWAYS READ `_spec.md` and its catalogs (`_user_stories.md`, `_dx.md`, `_uiux.md` when present, `_tests.md`) before starting
- REFERENCE `_spec.md` Part II for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — implement every test case assigned in ## Tests
</critical>

<requirements>
1. MUST follow `_uiux.md` exactly: `ExtensionFormatBadge` as a sixth `Pill mono size="xs"` variant in `extension-trust-badges.tsx` (copy `agent plugin`, default tone, never merged with trust badges); `ExtensionSkippedComponents` beside the kit inventory panel rendering the widened inventory payload's `diagnostics` (ingest code then live code, in order); badge on marketplace card meta row + detail cluster + installed card; payload truth beats feed marker.
2. MUST render the truthful states from `_user_stories.md`: badge absent on native (US-015.EC-2), Skipped section absent with zero diagnostics, fully-degraded explicit "0 resources ingested" line (US-015.EC-1), warning tone only for diagnostic rows — no danger tone, no invented states.
3. MUST add the optional `"format"` field to the extension feed entry as a hard cut at the current `manifest_version`: Go reader (`internal/marketplace/entry_extension.go`), in-repo feed `catalog/extensions.json`, fixtures, and daemon projection change together; no pre-cut fixture, no compatibility branch.
4. MUST reuse `@compozy/ui` primitives only (`Pill`, existing rows) — zero new generic primitives; domain components live in `web/src/systems/extensions/components/`; no subtitles/helper prose (design constraints in `_uiux.md`).
5. MUST leave the install and trust dialogs structurally untouched — the drifted-entry failure renders through the dialog's existing error path (copy-length check only).
6. MUST run frontend validation through Turborepo from the repo root (never package-local).
</requirements>

## Subtasks

- [x] 5.1 Go feed reader + `catalog/extensions.json` + fixtures + projection: `format` field hard cut
- [x] 5.2 `ExtensionFormatBadge` variant + wiring into settings/detail badge clusters
- [x] 5.3 Marketplace card meta-row badge + detail cluster + installed card (marker pre-install, payload truth installed)
- [x] 5.4 `ExtensionSkippedComponents` + kit-inventory integration (Format line, Skipped rows, degraded empty state)
- [x] 5.5 Vitest component suites (badge presence/absence, rows, card)
- [x] 5.6 Playwright journeys (badge → install → trust → installed → inventory; drifted-entry legible failure) — authored; execution deferred to the QA tail

## Implementation Details

Follow `_uiux.md` (surface map S1–S5 + component plan + signal mapping) and `_spec.md` Part II Data Models (feed hard cut). Existing anchors: `web/src/systems/marketplace/components/marketplace-card.tsx:22-105` (meta row), `marketplace-detail-extension*.tsx` + `marketplace-installed-card.tsx` (detail/installed), `web/src/systems/extensions/components/extension-trust-badges.tsx:40-43` ("distinct labels, never merged" rule — ours is a sixth), `extension-kit-inventory-panel.tsx:62-64,107` (kind groups), `web/src/systems/extensions/adapters/extensions-api.ts` + `hooks/use-extensions.ts` (payload consumption), `internal/marketplace/entry_extension.go:18-93` + `source.go:261` (strict entry decode), `web/e2e/` fixtures + specs (L-007: selectors/wait helpers ship with the new states in this same task).

Reference precedent: Hermes renders its portable pill only in the GUI (`.resources/hermes/apps/desktop/src/app/settings/plugins-settings.tsx:167`) — we ship it on every surface.

Skills to activate: `eng-design`, `ui-craft`, `impeccable`, `better-colors` (tone mapping only if contested), `eng-test-conventions` (Playwright shape), plus `web/CLAUDE.md` dispatch.

### Relevant Files

- `packages/ui/src/index.ts` — primitive inventory (reuse-before-create check: `Pill` covers everything)
- `web/src/systems/extensions/fixtures.ts` + `handlers.ts` — MSW fixtures gain format/diagnostics fields
- `web/src/generated/compozy-openapi.d.ts` — regenerated by task_04; source of the new field types
- `catalog/extensions.json` + `internal/marketplace/source_test.go` — feed + owning suite

### Dependent Files

- `web/src/routes/_app/settings/extensions.tsx` + `marketplace.extensions.tsx` — render paths hosting the badges
- `web/src/integrations/tanstack-query/reconcile-installed-extension.ts` — installed-card reconciliation carries format

### Related ADRs

- [ADR-002: Marketplace via existing feeds](adrs/adr-002.md) — the feed/badge contract
- [ADR-003: First-class diagnostics](adrs/adr-003.md) — the Skipped-section obligation

### Web/Docs Impact

- `web/`: `systems/extensions/components/{extension-trust-badges,extension-kit-inventory-panel}.tsx` + new `extension-skipped-components.tsx`, `systems/marketplace/components/{marketplace-card,marketplace-detail-extension,marketplace-installed-card}.tsx`, MSW fixtures, Vitest suites, `web/e2e` specs + shared selectors.
- `packages/site`: none — docs in task_06.
- QA impact: new behavior (badges, skipped rows, catalog entries) — add content-addressed `untested` scenario files (task_06 sweep; walked by task_08 incl. browser e2e).

### Extensibility / Agent Manageability / Config Lifecycle

- Extensibility: marketplace feed contract gains one optional field (registry surface); no hook/manifest/tool change (checked: surface registry, native tool set).
- Agent manageability: none new — web is a convenience surface; agent paths shipped in task_04 (checked: no new routes/verbs here).
- Config lifecycle: zero config keys; feed hard cut documented in Data Models (checked: marketplace config untouched — base URL/TTL as-is).

## Deliverables

- Badges + Skipped section live per `_uiux.md` on every named surface; feed entries with `format` flowing end-to-end
- Every test case assigned in `## Tests` implemented and passing **(REQUIRED)**
- `make gate` green (bun lanes + affected Go marketplace suite + Playwright lane)

## Tests

Cases assigned from `_tests.md`, the test contract — read each ID's full definition there before writing tests.

- [x] UT-048 — `ExtensionFormatBadge` render/absence
- [x] UT-049 — Skipped section rows, absence, fully-degraded zero-count line
- [x] UT-050 — marketplace card badge from feed marker
- [x] UT-051 — feed entry `format` decode at current version (hard cut)
- [x] IT-015 — marketplace flow: projection, web-vs-CLI install equivalence, marker-less entry, resource-only consent
- [x] E2E-004 — browser journey: card badge → dialogs → installed badge → inventory Skipped rows (authored)
- [x] E2E-005 — drifted catalog entry fails legibly in the dialog (authored)

## Success Criteria

- Every assigned test case implemented and passing
- Zero shadowed `@compozy/ui` exports (lint rule clean); zero new generic primitives
- Native extensions render with no format-specific signal anywhere (truthful absence)
- Frontend gates via Turborepo from repo root only
